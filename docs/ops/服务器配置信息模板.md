# 服务器配置信息（模板）

> 真实配置请写入项目根目录的 `服务器配置信息.md`（已在 `.gitignore` 忽略），避免泄露与误提交。

## 基本信息

- 服务器IP：
- SSH用户名：
- SSH端口：22
- 部署目录：/opt/naibao

## 后端配置

- 服务端口：8080
- 运行模式：release / debug
- 环境变量：参考 `backend/.env.example`

## 数据库（PostgreSQL）

- Host：
- Port：5432
- DB：
- User：
- SSLMode：disable / require
- 备份策略：每天/每周，保留周期

## Redis（可选）

- Host：
- Port：6379
- Password：

