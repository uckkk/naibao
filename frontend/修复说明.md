# 前端启动问题修复说明

## 🔧 已修复的问题

### 1. npm install 报错：Invalid tag name "#"

**原因**：package.json中可能有注释（JSON不支持注释）

**解决**：已清理package.json，移除所有注释

### 2. uni命令不存在

**原因**：uni-app CLI工具未正确安装

**解决**：改用vite直接启动（uni-app v3使用vite作为构建工具）

---

## ✅ 已更新的配置

1. **package.json**：
   - 移除了不存在的uni-app依赖
   - 使用vite作为开发服务器
   - 添加了正确的@dcloudio/vite-plugin-uni

2. **vite.config.js**：
   - 已创建Vite配置文件
   - 配置了uni-app插件
   - 设置了host: '0.0.0.0'（允许外部访问）

3. **启动脚本**：
   - `dev:h5` 改为使用 `vite --host`

---

## 🚀 现在可以启动了

```bash
cd /Users/stonem2/Documents/AiCode/naibao/frontend
npm install
npm run dev:h5
```

启动后会显示：
```
  ➜  Local:   http://localhost:5173/
  ➜  Network: http://192.168.x.x:5173/
```

**注意**：默认端口是5173，不是8080（8080被其他服务占用了）

---

## 📱 手机访问

在手机浏览器输入Network地址（例如：`http://192.168.1.100:5173`）

---

## ⚠️ 如果还有问题

### 问题1: 依赖安装失败

```bash
# 清理缓存重新安装
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### 问题2: 端口被占用

修改vite.config.js中的端口：
```javascript
server: {
  port: 3000, // 改为其他端口
}
```

### 问题3: 无法访问

确保：
- Mac防火墙允许该端口
- 手机和Mac在同一WiFi
- Network地址正确

---

**现在运行 `npm install` 然后 `npm run dev:h5` 即可！** 🎉


