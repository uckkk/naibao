# 前端启动说明

## 🚀 启动步骤

### 1. 安装依赖

```bash
cd /Users/stonem2/Documents/AiCode/naibao/frontend
npm install
```

### 2. 启动开发服务器

```bash
npm run dev:h5
```

### 3. 查看Network地址

启动后会显示类似：
```
  ➜  Local:   http://localhost:8080/
  ➜  Network: http://192.168.1.100:8080/
```

### 4. 手机访问

在手机浏览器输入Network地址（例如：`http://192.168.1.100:8080`）

---

## ⚠️ 如果遇到问题

### 问题1: npm install 报错

**解决**：删除node_modules和package-lock.json，重新安装
```bash
rm -rf node_modules package-lock.json
npm install
```

### 问题2: 端口8080被占用

**解决**：修改vite.config.js中的端口，或停止占用端口的进程
```bash
# 查看占用端口的进程
lsof -ti:8080

# 停止进程
kill -9 $(lsof -ti:8080)
```

### 问题3: 手机无法访问

**检查**：
- 手机和Mac是否在同一WiFi
- Mac防火墙是否允许8080端口
- Network地址是否正确

---

## 📝 已配置

- ✅ vite.config.js - Vite配置文件
- ✅ package.json - 已更新启动脚本
- ✅ API地址 - 已配置为服务器地址

---

**现在可以运行 `npm run dev:h5` 了！** 🎉


