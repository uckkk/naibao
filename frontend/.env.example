# 复制为 frontend/.env.local 并按需修改（该文件不提交）
#
# 生产环境建议使用域名 + HTTPS，并通过 Nginx 反向代理把 /api 转发到后端，
# 以降低跨域复杂度与提升移动端/小程序兼容性。

# 生产/多端构建使用（App/小程序等不经过 Vite dev server 时需要）
VITE_API_BASE_URL="http://localhost:8080"

# H5 开发使用：Vite dev server 会把 /api 反代到该后端地址
VITE_API_PROXY_TARGET="http://127.0.0.1:8080"
