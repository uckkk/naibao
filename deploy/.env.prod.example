# 生产部署示例配置（请复制为 deploy/.env.prod 并填入真实值）

# 站点域名（Caddy 自动申请 HTTPS 证书）
# - 如需仅 HTTP（不推荐，通知权限等会受限）：DOMAIN=:80
DOMAIN=naibao.example.com

# Postgres
POSTGRES_DB=naibao
POSTGRES_USER=naibao_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT（至少 32 字符随机）
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET

# 可选：管理员白名单（逗号分隔 user_id）
ADMIN_USER_IDS=

# 可选：CORS 白名单（逗号分隔）。同源部署可不填。
# CORS_ALLOW_ORIGINS=https://naibao.example.com
CORS_ALLOW_ORIGINS=

