# 本机托管（Home Hosting）示例配置
#
# 用途：
# - 后端 + 数据库跑在你家/公司机器（Docker）
# - 前端托管在 GitHub Pages（https://naibao.me）
# - 通过 Cloudflare Tunnel 把本机后端暴露为 https://api.naibao.me
#
# 复制为 deploy/.env.home 后按需修改

# Postgres
POSTGRES_DB=naibao
POSTGRES_USER=naibao_user
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# JWT（至少 32 字符随机）
JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET

# 管理员白名单（逗号分隔 user_id，可留空）
ADMIN_USER_IDS=

# CORS 白名单（逗号分隔）
# GitHub Pages + 自定义域名常见是：
# CORS_ALLOW_ORIGINS=https://naibao.me,https://www.naibao.me
CORS_ALLOW_ORIGINS=https://naibao.me,https://www.naibao.me

# 本机后端端口（宿主机端口映射到容器 8080）
# 说明：macOS 下 OrbStack 可能占用 8080，推荐默认用 18080。
NB_BACKEND_HOST_PORT=18080

# 前端对外域名（用于运营台自检/提示）
NB_PUBLIC_DOMAIN=naibao.me

# Cloudflare Tunnel（可选：用于脚本/运营台启动 tunnel）
# - named: 固定域名（推荐，配合 api.naibao.me）
# - quick: 临时 trycloudflare（不适合生产）
NB_TUNNEL_MODE=named
NB_TUNNEL_NAME=naibao-api
NB_TUNNEL_HOSTNAME=api.naibao.me
